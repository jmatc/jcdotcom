default:
  - build
  - 'connect:server'
  - watch
build:
  - createFeatureFile
  # - sass
  - hogan
  - js
dist:
  - build
  - committers
  - changelog
start:
  - build
  - 'connect:keepalive'
test:
  - jshint
  - jsonlint
js:
  - 'requirejs:<%= pkg.name %>-amd'
  - 'requirejs:<%= pkg.name %>-amd-min'